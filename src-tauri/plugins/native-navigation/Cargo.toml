[package]
name = "tauri-plugin-native-navigation"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-native-navigation"

[build-dependencies]
tauri-plugin = { version = "2.0.4", features = ["build"] }

[dependencies]
tauri = { version = "2.2.0" }
